# Module: nginx
# Docs: https://www.elastic.co/guide/en/beats/filebeat/8.4/filebeat-module-nginx.html

- module: nginx
  # Access logs
  access:
    enabled: true

    # Set custom paths for the log files. If left empty,
    # Filebeat will choose the paths depending on your OS.
    var.paths:
      - /usr/share/elastic-agent/data/elastic-agent-90167b/install/filebeat-8.4.3-linux-x86_64/elogs/nginx/tombola/access.log
      - /usr/share/elastic-agent/data/elastic-agent-90167b/install/filebeat-8.4.3-linux-x86_64/elogs/nginx/tombola/ad.access.log
      - /usr/share/elastic-agent/data/elastic-agent-90167b/install/filebeat-8.4.3-linux-x86_64/elogs/nginx/tombola/web.access.log
  # Error logs
  error:
    enabled: true

    # Set custom paths for the log files. If left empty,
    # Filebeat will choose the paths depending on your OS.
    var.paths:
      - /usr/share/elastic-agent/data/elastic-agent-90167b/install/filebeat-8.4.3-linux-x86_64/elogs/nginx/tombola/error.log
      - /usr/share/elastic-agent/data/elastic-agent-90167b/install/filebeat-8.4.3-linux-x86_64/elogs/nginx/tombola/ad.error.log
      - /usr/share/elastic-agent/data/elastic-agent-90167b/install/filebeat-8.4.3-linux-x86_64/elogs/nginx/tombola/web.error.log
  # Ingress-nginx controller logs. This is disabled by default. It could be used in Kubernetes environments to parse ingress-nginx logs
  ingress_controller:
    enabled: false

    # Set custom paths for the log files. If left empty,
    # Filebeat will choose the paths depending on your OS.
    #var.paths:
